//Function to find a particular entry by its position/index (index in the array)
var allCustomers=["priya","alka","pawan"];
  function findINdex(customers){
    var index=customers.indexOf("pawan"); // Assigning index to a variable.
    console.log(index);
  }
  
  findINdex(allCustomers);

//Function to find a particular entry by its position/index (index in the array)
var allCustomers=["priya","alka","pawan"];
  function findINdex(customers){
    var index=customers.indexOf("pawan"); // Assigning index to a variable.
    console.log(index);
  }
  
  findINdex(allCustomers);

//Function to find a particular entry by its position/index (index in the array)
var allCustomers=["priya","alka","pawan"];
  function findINdex(customers){
    var index=customers.indexOf("pawan"); // Assigning index to a variable.
    console.log(index);
  }
  
  findINdex(allCustomers);
